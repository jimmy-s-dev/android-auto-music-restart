# 검증 상태

## 재생 시작 기반 예방 복구 — 2026-09-09

Galaxy Z Fold6 / Android 16, Morphe YouTube Music 9.15.51에서 새 예방 방식을 구현하고 동일 서명으로 업데이트했다. 앱 버전은 **1.0.1 / versionCode 2**를 유지했다. 아래 이전 절들은 당시 구현의 기록이며 현재 자동 실행 정책은 README를 따른다.

| 항목 | 결과 |
|---|---|
| 릴리스 앱·기기 시험 APK 빌드 | 통과 |
| 단위 테스트 | 49개 통과, 실패·오류 0개 |
| Android Lint | 오류 0개, 경고 19개 |
| 서비스 전용 시작의 이력 | ABSENT 확인 |
| 실제 앱 화면 실행 후 홈 이동 | PRESENT 확인: 삼성 기기 hasShownUi 보완 적용 |
| 가상 화면 초기화 후 이력 | PRESENT 확인 |
| Shizuku 조회 연결 20회 | 대상 프로세스 교체·자동 재시작 없이 통과 |
| 서비스 재생부터 자동 복구 | 요청 한 번·실제 종료 한 번·새 세션 PLAYING 확인 |
| 사용자가 직접 강제 종료 후 홈 위젯 재생 | 자동 복구 한 번, 현재 곡·25곡 목록 유지 |
| 복구 후 현재 곡 장시간 관찰 | 동일 곡·세션으로 231초 이상 관찰, 이후 자연 곡 전환 |
| 다음 곡 독립 90초 관찰 | PASS, 전 구간 보안 잠금·화면 꺼짐 |
| 세 번째 곡 | 사용자가 1분 이상 청취했다고 보고; 별도 계측 결과와 구분 |
| 보안 잠금 상태에서 자동 복구 자체 | PASS, 22:08:19 시작 → 22:08:34 완료, 보안 잠금 true·화면 켜짐 false |
| Bluetooth 이어폰 분리 | 사용자 음악 정지 확인, PAUSED 이후 자동 재실행 없음 |
| 가상 초기화 도중 취소 | PASS, 즉시 중단·가상 화면 잔존 없음; 음악 정지 상태 확인 |

첫 위젯 시험에서는 사용자가 재시작 중 잠깐 잠금을 해제했으므로 그 시험을 잠금 유지 복구 성공으로 분류하지 않는다. 별도 보안 잠금 시험에서 완료 시점까지 잠금 유지와 화면 꺼짐을 확인했다. 현재 곡 231초 관찰 도중 보안 잠금으로 전환했으므로 231초 전체를 보안 잠금 재생으로 주장하지 않는다. 이어진 다음 곡 90초 관찰은 전 구간 보안 잠금이었다. AudioFlinger 활성 트랙 출력 프레임 증가와 사용자 청취 보고도 확인했다. 미디어 세션의 위치 증가만으로 실제 음향 출력을 보장하지는 않는다.

두 곡 통합 관찰기는 곡 전환 직후 일시적인 메타데이터 갱신을 추가 곡 변경으로 잘못 판정해 실패했다. 이는 음악 재생 실패로 판정하지 않았다. 관찰기에 PLAYING 상태의 3초 메타데이터 안정화를 적용하고 다음 곡을 독립적으로 90초 관찰해 통과했다. 수정된 통합 관찰기 전체 시나리오를 다시 통과했다고 주장하지 않는다.

첫 자동 시험의 반복 알림음은 Shizuku 서비스 제거·재연결 경쟁과 연결 실패 시 시도 미기록 문제였다. 서비스 재사용, 연결 해제 콜백 수정, 요청 수락 시 시도 저장, 무음 상태 알림을 적용했다. 수정 후 서비스·실제 위젯·보안 잠금 시험에서 반복 실행이나 연결 오류가 없었다. 상세 원인과 판별 보완은 [INVESTIGATION.md](INVESTIGATION.md)를 참고한다.

자동 중복 제한, PID 재사용, 보조 앱 재생성 후 미완료 복구 차단, 불명확 이력의 10초 기준과 자연 복구·곡 변경 초기화는 순수 로직 시험으로 확인했다. 모든 중단 조건을 실제 기기에서 각각 재현한 것은 아니다. 특히 이어폰 분리는 정상 재생 중 시험이며, 강제 종료와 가상 초기화 사이의 분리 시점까지 검증한 것은 아니다. 실차 연결·재연결, 새 구현의 재부팅 후 준비 복구, 실제 권한 상실 중단은 후속 기기 시험 대상으로 남긴다.

업데이트 전 APK를 사용자 홈 백업 폴더에 보관했다(SHA-256: `28e2d8410e02fbc9eb1241357ce80e15946d23e8b8e52e8f22290566277f8477`). 음악 앱·음악 설정·삼성 모드는 변경하지 않았다. 기기 시험에서는 자동화를 임시로 켰으며 시험 전 확인된 자동화 꺼짐 상태로 복원했다. 취소 시험 뒤 수동 복구로 재생을 확인한 다음, 사용자의 시험 종료 요청에 따라 대상 음악을 일시정지했다(`PASS: pausePlayback`). 이후 재생 시험은 실행하지 않았다. 로컬 원본 증거는 저장소에서 제외한 `dist/preventive-validation/`에 보관한다.

최종 설치 APK를 기기에서 다시 받아 빌드 산출물과 SHA-256 일치(`d136c2b40dcf38eb159f68db6e75c9710f28328c75fd5499ebb979871d9f4ff5`)를 확인했다. 원본·수정본 서명 인증서 SHA-256은 동일하다(`e5ccccf88d7ab52e04e3be3dc31946d76110b836eae01e436301599c8607480f`).

## 이전 구현 기록

후속 버퍼링 실험과 재현 상태 보존 기록은 [INVESTIGATION.md](INVESTIGATION.md)를 참고하세요. 아래 표와 설명은 각 시험 당시 소스와 기기 상태의 기록입니다.

## 서비스 전용 재생 순서 수정 재검증 — 2026-09-09

Galaxy Z Fold6 / Android 16, ADB `localhost:5555`, Morphe YouTube Music 9.15.51에서 시험했습니다. **버퍼링 중 종료 단계에 도달하지 못하던 문제는 수정했지만, 최종 음악 재생은 실패했습니다.** 실차 시험은 수행하지 않았습니다.

| 항목 | 이번 결과 |
|---|---|
| 기존 APK 보관·동일 서명 업데이트 | 통과, 앱 ID 유지, 시험용 수정본 설치 |
| 기존 기록·권한·설정 유지 | 확인, 자동화 꺼짐·차량 알림 미등록 유지 |
| 실제 메뉴 15초 시험, 화면 꺼짐 | BUFFERING 상태에서도 5초 후 대상 종료·새 세션 생성 확인 |
| 세션 없는 상태에서 시험 | 백그라운드 서비스 시작·5초 후 종료·새 세션 생성 확인 |
| 최종 PLAYING 및 재생 위치 증가 | 실패, 새 세션도 BUFFERING(position=59993) 지속 |
| 일시정지 상태 준비 | 대상 한정 pause 요청 후 PAUSED가 되지 않아 해당 조건 시험 불가 |
| 실제 기기 중복 요청·대기 중 취소 | PASS: cancel, 음악 추가 조작 없이 종료 |
| 단위 테스트 | 16개 통과: 기존 4개 + 실행 순서·실패 처리 12개 |
| 릴리스 앱·테스트 APK 빌드 / Lint | 통과 / 오류 0개, 경고 15개 |

실제 메뉴 시험 기록(KST):

- 17:44:24 수동 시험 시작 → 17:44:39 첫 재생 요청, BUFFERING(position=59504).
- 17:44:44 기존 PID 7833 종료 → 17:44:47 이전 PID·세션 없음, 대상 MusicBrowserService 시작. 새 PID 7187 확인.
- 17:45:03 최종 재생 확인 시간 초과, BUFFERING(position=59993). 반복 종료 없이 실패 기록.
- 세션 없는 상태의 17:46:07 시험에서도 PID 11575 종료 후 새 PID 12360을 만들었으나 최종 BUFFERING으로 실패.

각 새 프로세스의 `YT.onesie` 로그에서 `Fatal error occurred during Onesie request`와 `IO error Response code: 413`을 확인했습니다. 보조 앱의 onPlay 요청은 전달됐지만 스트리밍 요청 오류가 함께 발생했습니다. 413의 근본 원인 및 버퍼링과의 정확한 인과관계는 미확정이며, Morphe 설정·패치·계정·네트워크는 변경하지 않았습니다. 이번 결과를 재생 복구 성공으로 판정하지 않습니다.

음악 Activity 실행 없이 시험했으며 화면 꺼짐을 확인했습니다. 기기 probe는 `locked=false`, `screenOn=false`이므로 보안 잠금 재생은 입증하지 않았습니다. 청취 및 실차 검증은 미완료입니다.

기존 APK는 로컬 사용자 홈의 `android-auto-music-restart-backups/` 내 초기 설치 APK 백업에 보관했습니다(SHA-256: `5108e2fde6c708093a7eee069c150b4124f30d52c0cd4dc89ce8b2330a7a0668`). 기존 데이터 삭제나 재설치 없이 업데이트했습니다.

## 기기 검증 요약

Android 16 / API 36에서 Morphe YouTube Music과 Shizuku를 사용해 확인했던 과거 결과입니다. 아래 표는 서비스 전용 수정본의 재생 성공을 뜻하지 않으며, 이번 결과는 위 재검증 항목을 기준으로 합니다.

| 항목 | 결과 |
|---|---|
| 서명 APK 설치 및 업데이트 | 통과 |
| 중복 요청·대기 중 취소 | 통과 |
| 화면 꺼짐·잠금화면에서 백그라운드 재생/종료/재생 | 재생 동작과 청취 확인 |
| Shizuku 미실행 시 안전 중단 | 음악 조작 전에 중단 |
| 재부팅 후 알림 감지 서비스 복구 | 통과 |
| 재부팅 후 Shizuku 자동 시작 | Shizuku 자체 페어링 후 통과; 준비 지연 가능 |
| 대기 중 알림 접근 권한 해제 | 음악 조작 전에 중단 |
| 연결 해제에 따른 취소 | 기기 내 모의 신호 시험 통과 |
| 실제 차량 연결·장시간 재생·재연결 | 미검증 |

화면 꺼짐과 잠금화면 상태는 확인했지만, PIN 또는 생체 인증이 반드시 필요한 보안 잠금 상태까지 입증한 것은 아닙니다. 재부팅 복구 결과는 모든 기기나 네트워크에서의 자동 시작을 보장하지 않습니다. Wi-Fi 연결 지연과 차량 네트워크만 사용하는 조건은 미검증입니다.

## 현재 소스 검증

앱 ID와 테스트 실행기는 `com.jimmyshin.automusicrestart`를 사용합니다. 서비스 전용 수정본의 릴리스 앱 및 테스트 APK 빌드와 단위 테스트 16개가 통과했습니다. Android Lint는 오류 0개·경고 15개입니다. 앱의 기기 업데이트 설치와 실패 시 반복 종료 없이 중단하는 동작을 확인했습니다. 최종 재생 시험은 실패했고 실차 동작은 미검증입니다.

## 재현 절차와 남은 검증

1. README에 따라 서명을 설정하고 `./build.ps1`로 앱·테스트 APK 빌드, 단위 테스트, Lint를 실행합니다.
2. 기기 검증 시 본인이 지정한 기기에 두 APK를 설치하고 앱의 권한과 배터리 설정을 완료합니다. README의 `probe`, `cancel`, `sequence` 명령을 사용합니다.
3. 정차 상태에서 실제 Android Auto 연결 완료 알림을 등록합니다.
4. 화면을 끈 상태로 자동 재시작 후 3분 이상 재생하고 버퍼링 여부를 확인합니다.
5. 알림 갱신 시 중복 실행 없음, 연결 해제 시 대기 작업 중단, 재연결 시 한 번 실행됨을 확인합니다.
6. 신뢰하는 Wi-Fi에서 차량 무선 연결로 전환해도 Shizuku가 유지되는지 확인합니다.

실차 검증 전이므로 차량 버퍼링 해결 여부는 미확인입니다.

## 가상 화면 초기화 검증 — 2026-09-09

이 절은 위 초기 구현·서비스 전용 수정의 과거 실패/설정 상태와 구분한 후속 결과입니다. 가상 화면 초기화 시험용 보조 앱을 같은 서명으로 업데이트 설치했습니다. 현재 자동화 사용 및 기존 차량 알림 등록은 모두 true이며 유지했습니다.

- 릴리스 APK·테스트 APK 빌드 성공, 단위 테스트 31개 통과, Lint 오류 0개·경고 20개.
- 최초 실제 BUFFERING 상태에서 임시 가상 화면의 Activity 초기화 후 재생 복구. 가상 화면 제거 뒤 185초 고정 곡 관찰 통과, AudioFlinger 출력 프레임 증가 확인.
- 사용자에게 일반 보안 잠금을 요청한 뒤 isDeviceLocked=true / screenOn=false에서 앱 통합 경로 재생 확인. 약 204초까지 재생 후 자연스럽게 다음 곡도 재생됐습니다. 이때 별도 185초 고정 곡 관찰은 늦게 시작돼 자연스러운 곡 전환으로 FAIL이며, 정식 통과와 구분합니다.
- 실제 가상 화면 생성 중 사용자 취소를 모의한 기기 시험 통과. Runner 종료 및 가상 화면 제거, 후속 재생 단계 차단 확인.
- 현재 곡이 같아도 Morphe가 다음 곡 목록을 재구성하는 경우를 확인했습니다. 사용자가 현재 곡 재생을 우선하고 목록 재구성을 허용해 최종 정책에 반영했습니다.

최종 메뉴 시험과 세션 없는 시작 조건은 아래 추가 기록을 확인하세요. 상세 원인, 실패 실험, 사용자 승인, 백업·복원 조건은 [INVESTIGATION.md](INVESTIGATION.md)에 기록했습니다. 새 방식의 재부팅 및 실차 연결·재연결은 아직 검증하지 않았습니다.

### 최종 메뉴 결과

`menuSequence` 통과: 실제 메뉴 버튼에 instrumentation 클릭을 전달해 15초 대기를 수행했고, 새 세션 PLAYING 및 가상 화면 제거 후 185초 동안 같은 곡·세션·초기화 후 목록이 유지됐습니다. 보안 잠금=true, 화면 켜짐=false였으며 AudioFlinger 출력 프레임 증가도 확인했습니다. 재시작 전 다음 곡 목록은 사용자 승인대로 재구성을 허용했습니다.

실기기 시험 당시 APK SHA-256(현재 빌드 산출물과 별개): `f42e149865f5faa18089686dd101f3ca5c0d5ef3ecf7432a9356ede992240a7e`.

세션 없는 시작 조건도 `PASS: sequence`로 통과했습니다. 첫 서비스 요청 뒤 기존 PID·세션 제거, 가상 화면 초기화 후 새 PID 16895의 PLAYING을 확인했습니다. 초기 서비스는 목록을 공개하지 않아 전체 목록 비교 불가를 기록했고, 현재 곡 비교는 통과했습니다. 최종 빌드의 중복 요청·대기 중 취소도 통과했으며 임시 가상 화면 잔존은 없었습니다.

최종 설치 APK가 검증 빌드와 SHA-256까지 일치함을 확인했습니다. 시험 전용 APK와 기기에 올린 임시 진단 dex 파일은 제거했고, 시험한 보조 앱 및 음악 재생은 유지했습니다.
